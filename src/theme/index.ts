import { MD3LightTheme } from "react-native-paper";

const theme = {
    ...MD3LightTheme,
    colors: {
      ...MD3LightTheme.colors,
      primary: '#007AFF', // Cor principal
      secondary: '#0055FF', // Cor secundária
    },
  };